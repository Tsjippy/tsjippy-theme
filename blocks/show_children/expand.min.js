document.addEventListener("DOMContentLoaded", () => {
  (document
    .querySelectorAll(
      ".childpost .current_page_ancestor > .children.hidden, .current_page_item > .children.hidden",
    )
    .forEach((e) => e.classList.remove("hidden")),
    (button = document.createElement("button")),
    (button.innerHTML = "&#x25BC;"),
    button.classList.add("button"),
    button.classList.add("small"),
    button.classList.add("expand-children"),
    document
      .querySelectorAll(".childpost .page_item_has_children > a")
      .forEach((e) => {
        null != e.parentNode.querySelector(".children") &&
          e.nextElementSibling.matches(":not(.expand-children)") &&
          e.parentNode.insertBefore(button.cloneNode(!0), e.nextSibling);
      }),
    document.addEventListener("click", (e) => {
      e.target.matches(".expand-children") &&
        e.target
          .closest("li")
          .querySelector(".children")
          .classList.toggle("hidden");
    }));
});
//# sourceMappingURL=expand.min.js.map
